module jupiter_peripherals_stub
(
    input wire clk,
    input wire reset
);

    // Milestone 1 boundary placeholder only.
    // Controller, timer, interrupt, and peripheral interfaces remain TBD.

endmodule
